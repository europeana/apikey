server:
  port: 8888

security:
  oauth2:
    client:
      clientId: test
      clientSecret: test
      scope: read,write
      # enable auto-approve to skip the expliciet approval step
      # auto-approve-scopes: '.*'

logging:
  level:
    org.springframework.security: DEBUG
    org.springframework.web: DEBUG